# Contributors

This file records individual ownership of the group project's modules. Git history remains the authoritative record of each change.

## Dewald Allers - Member 1: Core & Interface

Dewald Allers contributed the following components:

- LP/IP domain model and shared solver contracts
- Arbitrary-size input text-file parser and line-specific validation
- Canonical-form conversion for max/min objectives, all constraint relations, and all required variable restrictions
- Original-model and canonical-form display formatting
- Output text-file generation with fixed three-decimal formatting
- Menu-driven WinForms input, algorithm-selection, iteration, sensitivity-integration, and export workflow
- User-facing file, validation, compatibility, unavailable-algorithm, unbounded-model, and unexpected-error handling
- `solve.exe` build naming
- Sample input files and the dependency-free Member 1 verification project

Every new Member 1 source file also contains a `Contributor: Dewald Allers` header.
